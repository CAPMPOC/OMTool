<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:form="sap.ui.layout.form"
    xmlns:l="sap.ui.layout">

    <Dialog
        id="viewEmployeeView"
        title="View Employee"
        contentWidth="500px"
        contentHeight="auto"
        resizable="false"
        draggable="true">

        <content>
            <VBox class="sapUiSmallMarginBegin sapUiSmallMarginTop">
                <Title  
                        text="Basic Information" 
                        level="H4"
                        class="sapUiSmallMarginBottom"/>
                <!-- viewEmployee ID -->
                <VBox class="sapUiSmallMarginBottom">
                    <Label text="Employee ID" required="true"/>
                    <Input value="{viewEmployee>/Empid}"
                        width="98%"
                        placeholder="" enabled="false"/>
                </VBox>
                
                <HBox class="sapUiSmallMarginBottom">
                <VBox class="sapUiSmallMarginEnd" width="48%">
                    <Label  id="vboxvFirstName" text="First Name" required="true"/>
                    <Input id="inputvFirstName" value="{viewEmployee>/FirstName}"
                        width="100%"
                        placeholder="" enabled="false"/>
                </VBox>
                <VBox class="sapUiSmallMarginEnd" width="48%">
                    <Label text="Last Name" required="true"/>
                    <Input
                        value="{viewEmployee>/LastName}"
                        width="100%"
                        placeholder="" enabled="false"/>
                </VBox>
                </HBox>
                
                <!-- Address -->
                <HBox >
                <VBox width="48%" class="sapUiSmallMarginEnd">
                    <Label text="Location"/>
                    <Input
                        value="{viewEmployee>/LocationVHData/LocDesc}"
                        width="100%"
                        placeholder="" enabled="false"/>
                </VBox>

                <VBox width="48%" class="sapUiSmallMarginEnd">
                    <Label text="C-User ID"/>
                    <Input
                        value="{viewEmployee>/CID}"
                        width="100%"
                        placeholder="" enabled="false"/>
                </VBox>
                </HBox>
                
                <!-- Date Fields Row -->
                <Title
                        text="Assignment" 
                        level="H4"
                        class="sapUiSmallMarginBottom sapUiSmallMarginTop"/>

                         <!-- Employer Name -->
                <VBox>
                    <Label
                        text="Employer Name" />
                    <Input class="sapUiSmallMarginBottom"
                        value="{viewEmployee>/Employer}"
                        width="98%"
                        placeholder="" enabled="false"/>
                </VBox>
                    <HBox class="sapUiSmallMarginBottom">
                  <VBox width="48%" class="sapUiSmallMarginEnd">
                    <Label text="Product Group"/>
                    <Input 
                        value="{viewEmployee>/ProductGroup}"
                        width="100%"
                        placeholder="" enabled="false"/>
                </VBox>

                <VBox width="48%" class="sapUiSmallMarginEnd">
                    <Label text="Service Group"/>
                    <Input value="{viewEmployee>/ServiceGroup}"
                        width="100%"
                        placeholder="" enabled="false"/>
                </VBox>
                </HBox>

                <HBox>
                <VBox  width="48%" class="sapUiSmallMarginEnd">
                    <Label text="Product"/>
                    <Input 
                        value="{viewEmployee>/Product}"
                        width="100%"
                        placeholder="" enabled="false"/>
                </VBox>   

                <VBox  width="48%" class="sapUiSmallMarginEnd">
                    <Label text="Accessibility"/>
                    <Input value="{viewEmployee>/Accessibility_AccessID}"
                        width="100%"
                        placeholder="" enabled="false"/>
                </VBox>      
                </HBox>
                <HBox class="sapUiSmallMarginTop">
                    
                    <!-- Kick-off Date -->
                    <VBox width="48%" class="sapUiSmallMarginEnd">
                    
                        <Label text="Roll-On Date" required="true"/>
                        <DatePicker value="{viewEmployee>/RollOnDate}"
                            valueFormat="yyyy-MM-dd"
                            displayFormat="MM/dd/yyyy"
                            width="100%"
                            placeholder="mm/dd/yyyy"
                            editable="false"/>
                    </VBox>
                    
                    <!-- Roll-off Date -->
                    <VBox width="48%" class="sapUiSmallMarginEnd">
                        <Label text="Roll-off Date"/>
                        <DatePicker value="{viewEmployee>/RollOffDate}"
                            valueFormat="yyyy-MM-dd"
                            displayFormat="MM/dd/yyyy"
                            width="100%"
                            placeholder="mm/dd/yyyy" editable="false"/>
                    </VBox>
                </HBox>
                
                <!-- EXPERIENCE SECTION -->
                <Title text="Experience"
                    level="H4"
                    class="sapUiSmallMarginBottom sapUiSmallMarginTop" />
                
                <!-- SAP and Non-SAP Experience -->
                <HBox class="sapUiSmallMarginBottom">
                    <VBox width="30%"
                        class="sapUiSmallMarginEnd">
                        <Label text="SAP Experience"
                            required="true" />
                        <Input value="{viewEmployee>/SAP}"
                            width="100%" enabled="false"/>
                    </VBox>
                    
                    <VBox width="30%"
                        class="sapUiSmallMarginEnd">
                        <Label id="lblNonSAPExp"
                            text="Non-SAP Experience" />
                        <Input id="inputNonSAPExp"
                            value="{viewEmployee>/NonSAP}"
                            width="100%" enabled="false" />
                    </VBox>

                    <VBox width="30%"
                        class="sapUiSmallMarginEnd">
                        <Label text="Total Experience" />
                        <Input value="{viewEmployee>/SAPToday}"
                            width="100%"
                            placeholder=""  enabled="false"/>
                    </VBox>
                </HBox>
                
                <!-- Total Experience and Skills -->
                <!-- <HBox id="hboxTotalExpSkills"> -->
                    
                    
                    <VBox>
                        <Label text="Employee Skills" />
                        <Input value="{viewEmployee>/Skill_SkillID}"
                            width="98%"
                            placeholder="" enabled="false"/>
                    </VBox>
                <!-- </HBox> -->
                
                
                <!-- Status Tracking Section -->
                <VBox class="sapUiMediumMarginTop">
                    
                    <!-- Status Tracking Title -->
                    <Title text="Status Tracking" 
                        level="H4"/>
                    <HBox>
                    <HBox justifyContent="Start"
                        alignItems="Center"
                        width="50%"
                        class="sapUiSmallMarginTop">
                        <Text text="New Employee" />
                        <Switch
                            state="{viewEmployee>/isNewRecord}"  customTextOn="Yes" customTextOff="No" enabled="false"/>
                    </HBox>
                    
                    <HBox justifyContent="Start"
                        alignItems="Center"
                        width="50%"
                        class="sapUiSmallMarginTop">
                        <Text text="Onboarding KT Begun" />
                        <Switch state="{viewEmployee>/handoverKtBegun}"  customTextOn="Yes" customTextOff="No" enabled="false"/>
                    </HBox>
                </HBox>
                    
                </VBox>
                
            </VBox>
        </content>
        <endButton>
            <Button
                text="Close"
                press="onCloseviewEmployeeDetail"
                icon="sap-icon://decline"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>